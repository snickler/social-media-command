<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="using:SocialMediaCommander.Desktop.ViewModels"
             mc:Ignorable="d" d:DesignWidth="400" d:DesignHeight="800"
             x:Class="SocialMediaCommander.Desktop.Views.SocialFeedView"
             x:DataType="vm:SocialFeedViewModel">

    <Border Background="{DynamicResource SMC-Card-Background}" 
            BorderBrush="{DynamicResource SMC-Card-Border}" 
            BorderThickness="1" 
            CornerRadius="8">
        
        <Grid RowDefinitions="Auto, Auto, *">
            <!-- Header -->
            <Grid Grid.Row="0" Margin="16,16,16,10" ColumnDefinitions="*,Auto">
                <TextBlock Text="Social Feeds" FontSize="15" FontWeight="SemiBold" VerticalAlignment="Center"/>
                <Button Grid.Column="1" Content="🔄 Refresh" Classes="ghost" IsEnabled="{Binding !IsRefreshing}"/>
            </Grid>

            <!-- Platform Filters -->
            <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled" Margin="16,0,16,10">
                <ItemsControl ItemsSource="{Binding PlatformFilters}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal" Spacing="4"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Content="{Binding Name}" Classes="outline"
                                    Command="{Binding $parent[UserControl].((vm:SocialFeedViewModel)DataContext).FilterByPlatformCommand}"
                                    CommandParameter="{Binding Id}"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <!-- Feed Content -->
            <ScrollViewer Grid.Row="2">
                <StackPanel Spacing="12" Margin="16,0,16,16">
                    <!-- Loading State -->
                    <StackPanel IsVisible="{Binding IsLoading}" Spacing="10" MinHeight="300" 
                                HorizontalAlignment="Center" VerticalAlignment="Center">
                        <ProgressBar IsIndeterminate="True" Width="100"/>
                        <TextBlock Text="Loading social feeds..." Opacity="0.7" HorizontalAlignment="Center"/>
                    </StackPanel>

                    <!-- Empty State -->
                     <StackPanel IsVisible="{Binding !HasFeedItems}" Spacing="10" MinHeight="300"
                                 HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBlock Text="📭" FontSize="32" HorizontalAlignment="Center"/>
                        <TextBlock Text="No posts to show" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                        <TextBlock Text="Posts from connected accounts will appear here" Opacity="0.7" HorizontalAlignment="Center"/>
                    </StackPanel>

                    <!-- Feed Items -->
                    <ItemsControl ItemsSource="{Binding FeedPosts}" IsVisible="{Binding HasFeedItems}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="{DynamicResource SMC-Card-Border}" BorderThickness="1" CornerRadius="6" Padding="12">
                                    <StackPanel Spacing="6">
                                        <!-- Header -->
                                        <Grid ColumnDefinitions="Auto,*,Auto">
                                            <Border Width="28" Height="28" CornerRadius="14" Background="Gray" Margin="0,0,10,0">
                                                <!-- Placeholder for Avatar -->
                                            </Border>
                                            <StackPanel Grid.Column="1" Spacing="2">
                                                <TextBlock Text="{Binding UserName}" FontWeight="SemiBold"/>
                                                <TextBlock Text="{Binding TimeAgo}" FontSize="12" Opacity="0.7"/>
                                            </StackPanel>
                                            <TextBlock Grid.Column="2" Text="{Binding PlatformDisplayName}" FontSize="12" Opacity="0.7"/>
                                        </Grid>
                                        <!-- Content -->
                                        <TextBlock Text="{Binding Content}" TextWrapping="Wrap"/>
                                        <!-- Media Placeholder (if any) -->
                                        <Border IsVisible="{Binding HasMedia}" Background="#F0F0F0" Height="80" CornerRadius="4">
                                            <TextBlock Text="[Media Content]" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Border>
</UserControl> 